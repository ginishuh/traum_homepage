# Decap CMS OAuth proxy env (do not commit .env)
PORT=3000
# Register a GitHub OAuth App and set below:
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Callback URL must match: https://blog.trr.co.kr/oauth/callback
OAUTH_REDIRECT_URL=https://blog.trr.co.kr/oauth/callback
# Allowed origins for postMessage back to CMS admin UI
ALLOWED_ORIGINS=https://blog.trr.co.kr
# GitHub token scope for Decap CMS commits
# - 공개 저장소: public_repo (최소 권한)
# - 비공개 저장소: repo
GITHUB_SCOPE=public_repo

# 개발 편의 옵션: 메시지 postMessage 대상 오리진을 '*'로 허용
# 로컬 테스트에만 사용하세요. 운영에서는 0 유지
DEV_ALLOW_ALL_ORIGINS=0

# --- Local development example (옵션 A) ---
# OAUTH_REDIRECT_URL=http://localhost:17177/oauth/callback
# ALLOWED_ORIGINS=http://localhost:17177,http://127.0.0.1:17177
# DEV_ALLOW_ALL_ORIGINS=1

# Optional: HTTP Basic Auth for admin/OAuth gate (if enabled in Nginx)
BASIC_AUTH_USER=
BASIC_AUTH_PASS=

# (로컬 전용 UID/GID 옵션 제거됨)
