# 개발 환경용 오버라이드
# 프로덕션에서는 이 파일을 사용하지 않음

services:
  blog:
    # 개발 시 로컬 파일 변경을 즉시 반영하려면 주석 해제
    # volumes:
    #   - ./public:/usr/share/nginx/html:ro
    
    # 개발 모드에서는 항상 재빌드
    build:
      context: .
      args:
        HUGO_ENV: development
